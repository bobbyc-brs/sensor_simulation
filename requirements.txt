# Runtime dependency for map rendering (web and desktop visualizers).
matplotlib>=3.8

# Natural Earth basemap (NaturalEarthMap).
# Pillow: raster image crop/load. cartopy+shapely: vector fallback if no raster image present.
Pillow>=10.0
cartopy>=0.22

# All other components use the Python standard library only (socket, subprocess, http.server, etc.).